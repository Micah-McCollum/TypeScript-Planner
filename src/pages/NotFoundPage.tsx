import React from "react";
import { Box } from "@mui/material";

const NotFoundPage: React.FC = () => {
    return (
    <Box sx={{ marginLeft: "0px", padding: "16px" }}>  
      <h2>NOT FOUND...</h2>
    </Box>
     );
}

export default NotFoundPage;
